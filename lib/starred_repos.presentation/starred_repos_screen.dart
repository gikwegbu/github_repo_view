import 'package:flutter/material.dart';

class StarredRepoScreen extends StatelessWidget {
  const StarredRepoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
